/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["www.salnet.xyz","salnet.xyz","www.sahilwise.com", "sahilwise.com"],
       
    }
};

export default nextConfig;